{
  "name": "vintage-bundles",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {},
  "devDependencies": {
    "gh-pages": "^2.1.1"
  },
  "scripts": {
    "start": "reveal-md index.md --watch --scripts index.js",
    "build": "rm -rf build && reveal-md index.md --static build --static-dirs=images && mkdir build/_assets && cp style.css build/_assets && cp -a fonts/. build/_assets/fonts/ && touch build/.nojekyll",
    "deploy": "gh-pages -d build --dotfiles",
    "print": "reveal-md index.md --print slides.pdf --print-size 1024x768"
  },
  "author": "Sia Karamalegos",
  "license": "CC-BY-NC-SA-4.0"
}
